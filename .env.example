# Xray gRPC API 地址（公网）
XRAY_API_ADDR=67.209.178.119:80
# 写操作鉴权 token，留空则所有写操作返回 403
ADMIN_TOKEN=
# 宿主机映射端口（可选，默认 8090）
HOST_PORT=8090
# 每小时流量持久化 JSON 文件路径（默认 ./data/hourly.json）
HOURLY_DATA_FILE=./data/hourly.json
# 每小时流量后台采样间隔（秒，默认 60）
SAMPLE_INTERVAL=60
# 每小时流量历史保留天数（默认 30）
RETENTION_DAYS=30
